import { createStore } from "vuex";
import postsModule from "./modules/postsModule";

export default createStore({
	state: {},
	getters: {},
	mutations: {},
	actions: {},
	modules: {
		postsModule
	},
});
